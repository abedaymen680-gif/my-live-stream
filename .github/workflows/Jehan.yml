name: Quick 1 Minute IPTV

on:
  workflow_dispatch:

jobs:
  one_minute_job:
    runs-on: ubuntu-latest
    steps:
      - name: Install FFmpeg
        run: sudo apt-get update && sudo apt-get install -y ffmpeg

      - name: Download 1 Minute
        run: |
          # هنا تم ضبط الوقت على 60 ثانية فقط (-t 60)
          ffmpeg -reconnect 1 -reconnect_at_eof 1 -reconnect_streamed 1 \
          -i "http://5-host.xyz:8080/live/177716153252/17laOe555B/431637.m3u8" \
          -t 60 -c copy -user_agent "Mozilla/5.0" minute_video.mp4

      - name: Send to Telegram
        run: |
          # إرسال الملف (سيكون حجمه صغيراً ولن يرفضه تليجرام)
          curl -v -F "video=@minute_video.mp4" -F "caption=فيديو الدقيقة الواحدة 🚀" \
          "https://api.telegram.org/bot8553237214:AAGnvhfyXa-B5Wkm3g_VQTRVbbr0ShyYM0U/sendVideo?chat_id=6849625315"
          
